server:
  port: 8087

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/db_cotizaciones?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

ms:
  usuarios:
    url: ${MS_USUARIOS_URL:http://localhost:8083}
  componentes:
    url: ${MS_COMPONENTES_URL:http://localhost:8085}